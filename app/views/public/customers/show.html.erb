<div class="container takasa">

  <div class="row">
	<h2 class="mypage"><i class="fas fa-user"></i>マイページ</h2>

	<p class="under_mypage"><strong>登録情報</strong><%= link_to "編集する", edit_customers_path(@customer.id), class:"btn btn-primary mypage_edit" %>
	</p>
  </div>
		<div class="row">
			<div class="col-xs-8">
				<table class="table table-bordered">
					<tr>
						<td>氏名</td>
						<td><%= @customer.last_name %><%= @customer.first_name %></td>
					</tr>
					<tr>
						<td>カナ</td>
						<td><%= @customer.last_name_kana %><%= @customer.first_name_kana %></td>
					</tr>
					<tr>
						<td>郵便番号</td>
						<td><%= @customer.postal_code %></td>
					</tr>
					<tr>
						<td>住所</td>
						<td><%= @customer.address %></td>
					</tr>
					<tr>
						<td>電話番号</td>
						<td><%= @customer.telephone_number %></td>
					</tr>
					<tr>
						<td>メールアドレス</td>
						<td><%= @customer.email %></td>
					</tr>
				</table>
			</div><br><br>
		</div>
		<p><strong>配送先　　</strong><%= link_to "一覧を見る", addresses_path, class:"btn btn-primary" %>
		<p><strong>注文履歴　</strong><%= link_to "一覧を見る", orders_path, class:"btn btn-primary" %>
	</div>