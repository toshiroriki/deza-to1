<div class="container takasa">
  <div class="row">
    <div class="col-xs-3">
      <%= flash[:error] %>
    </div>

    <div class="col-xs-6">
      <h2>ログイン</h2>
      <br>
      <br>
      <h2 style="width: 460px;">会員の方はこちらからログイン</h2>
      <br>
      <br>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.label :メールアドレス %>　　　　　<%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div><br><br>

        <div class="field">
          <%= f.label :パスワード %>　　　　　　　<%= f.password_field :password, autocomplete: "current-password" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>



        <div class="actions">
          <%= f.submit "ログイン", class: "btn btn-primary btn-lg center-block" %>
        </div>
      <% end %>

      <br>
      <h3 style="width: 300px;background-color: #a6d3c8; text-align: center; color: white">会員登録がお済みでない方</h3>
      <p><%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください</p><br>
    </div>
  </div>
</div>
